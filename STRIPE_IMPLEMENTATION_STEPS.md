# Stripe実装 - ステップバイステップガイド

## 現在の状況
- ✅ ドキュメント作成完了
- ❌ 実装はまだ

---

## 次にやること（3つのステップ）

### ステップ1: Stripeのシークレットキーを取得（あなたがやること）

#### 1-1. Stripeダッシュボードにアクセス
```
URL: https://dashboard.stripe.com
```

#### 1-2. ログイン
- メールアドレスとパスワードでログイン

#### 1-3. APIキーページに移動
```
左メニュー → 「開発者」→「APIキー」
または直接: https://dashboard.stripe.com/apikeys
```

#### 1-4. ライブシークレットキーをコピー
- 画面に表示されているキーをコピー
- 形式: `sk_live_` で始まる長い文字列

#### 1-5. 以下をお教えください
```
「ライブシークレットキーを取得しました」
とお知らせください

（キー全体は送信しないでください。
 先頭の「sk_live_」が見えれば十分です）
```

---

### ステップ2: 環境変数に設定（私がやること）

#### 2-1. Manus WebDev Secrets に設定
```
1. Manus WebDev 管理画面を開く
2. 「Settings」→「Secrets」をクリック
3. 「+ Add Secret」をクリック
4. Key: STRIPE_SECRET_KEY
5. Value: sk_live_xxxxxxxxxxxxx（あなたが取得したキー）
6. 「Save」をクリック
```

#### 2-2. 確認
```
✅ キーが暗号化されて保存される
✅ ソースコードに露出しない
✅ ログに記録されない
```

---

### ステップ3: バックエンドコードを実装（私がやること）

#### 3-1. Stripeライブラリをインストール
```bash
npm install stripe
```

#### 3-2. バックエンドコードを修正
```typescript
// server/routes/stripe.ts を修正
// 実際のStripe API呼び出しを追加
```

#### 3-3. テスト実行
```bash
npm test
```

---

## 実装の全体フロー図

```
┌─────────────────────────────────────────────────────┐
│ ステップ1: あなたがやること                          │
│ Stripeダッシュボードからキーを取得                   │
│ 「ライブシークレットキーを取得しました」と報告       │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│ ステップ2: 私がやること                              │
│ Manus WebDev Secrets にキーを設定                   │
│ 環境変数から読み込めるようにする                     │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│ ステップ3: 私がやること                              │
│ バックエンドコードを実装                             │
│ 実際のStripe API呼び出しを追加                      │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│ ステップ4: 私がやること                              │
│ テスト実行                                           │
│ 決済フローが正常に動作することを確認                 │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│ 完了                                                 │
│ Stripe機能実装完了                                  │
└─────────────────────────────────────────────────────┘
```

---

## 今すぐやることリスト

### あなたがやること（今すぐ）
- [ ] Stripeダッシュボード (https://dashboard.stripe.com) にアクセス
- [ ] 「開発者」→「APIキー」に移動
- [ ] ライブシークレットキーをコピー
- [ ] 「ライブシークレットキーを取得しました」と報告

### 報告後、私がやること
- [ ] Manus WebDev Secrets にキーを設定
- [ ] Stripeライブラリをインストール
- [ ] バックエンドコードを実装
- [ ] テスト実行
- [ ] 完了報告

---

## 重要な注意事項

### 🔴 絶対にしないこと
1. **キー全体をチャットに貼り付けない**
   - 記録に残る
   - 永遠に流出する

2. **メールで送信しない**
   - 記録に残る
   - 永遠に流出する

3. **スクリーンショットを撮らない**
   - 画像に含まれる
   - 流出リスク

### ✅ 安全な報告方法
```
「ライブシークレットキーを取得しました。
 先頭は sk_live_ です」

と報告してください。
```

---

## よくある質問

### Q1: キーはどこから取得しますか？
**A:** Stripeダッシュボール → 「開発者」→「APIキー」

### Q2: キーの形式は？
**A:** `sk_live_` で始まる長い文字列（例：`sk_live_51Hs7ZBxxxxxxxxxxxxxx`）

### Q3: キーを間違えて送信してしまった場合は？
**A:** 直ちにStripeダッシュボードでそのキーを削除し、新しいキーを生成してください。

### Q4: 実装にはどのくらい時間がかかりますか？
**A:** キーを取得いただければ、30分～1時間で実装完了予定です。

### Q5: テストはどのように行いますか？
**A:** テストクレジットカード番号を使用して、実際の決済フローをテストします。

---

## 次のアクション

**あなたがやること：**

1. Stripeダッシュボールにアクセス: https://dashboard.stripe.com
2. ライブシークレットキーを取得
3. 以下のように報告してください：

```
「ライブシークレットキーを取得しました。
 先頭は sk_live_ です。
 実装してください。」
```

**その後、私が以下を実施します：**

1. Manus WebDev Secrets にキーを設定
2. Stripeライブラリをインストール
3. バックエンドコードを実装
4. テスト実行
5. 完了報告

---

## 実装完了後に提供する内容

1. **実装レポート**
   - 実装内容の説明
   - セキュリティ対策の確認

2. **テストレポート**
   - テスト結果
   - 動作確認

3. **運用ガイド**
   - 日常的な運用方法
   - トラブルシューティング

---

**ご不明な点があれば、お気軽にお問い合わせください。**
