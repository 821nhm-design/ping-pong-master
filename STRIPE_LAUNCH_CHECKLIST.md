# Ping Pong Master 買い切り販売ローンチチェックリスト

> この文書は実装・公開前の作業用資料です。法的な最終判断は、販売地域に詳しい弁護士・行政書士・税理士等へ確認してください。

## 実装済み

| 項目 | 状態 |
|---|---|
| 買い切り用のStripe Checkoutモード | 実装済み（`mode: payment`） |
| 商品識別子 | `pro` |
| 価格のドラフト | JPY 1,500（最終価格の確認が必要） |
| 決済成功・キャンセル画面 | 実装済み |
| Stripeセッションの支払状態検証API | 実装済み |
| Stripe Webhookルートの登録 | 実装済み |
| Webhook署名検証用の生ボディ受信 | 実装済み |
| 利用規約・プライバシーポリシー・特商法表記 | 作業用ドラフトを画面へ追加済み |

## 公開前に必須の設定

1. Stripeのテスト環境で `STRIPE_SECRET_KEY` をホスティング環境のSecretとして設定する。リポジトリへ実キーをコミットしない。
2. Stripe DashboardでWebhookエンドポイントを `/api/stripe/webhook` に登録し、`checkout.session.completed` など必要なイベントを選び、発行された署名秘密鍵を `STRIPE_WEBHOOK_SECRET` に設定する。
3. `FRONTEND_URL` を実際の本番ドメインへ設定する。現在の一時プレビューURLは本番用に使用しない。
4. Stripeテストカードで、購入画面、成功画面、キャンセル画面、Webhook到達、プレミアム解放、二重処理防止を確認する。
5. 販売者名、責任者、住所、電話番号、公開用メールアドレス、最終価格、返金条件、対応環境を法的ページへ記入する。
6. 日本向けには特定商取引法、消費者契約法、電子消費者契約法、消費税・インボイス等を確認する。
7. 海外向けには販売地域ごとの消費者取消権、デジタルコンテンツ提供条件、VAT/GST、プライバシー法、輸出・制裁規制等を確認する。日本の特商法表記だけで海外販売の要件を満たすとは限らない。
8. Stripeで本番モードへ切り替える前に、最終価格と販売地域を明示的に承認する。

## 現時点で未確定のため、ユーザー確認が必要な項目

- 最終販売価格（現在のコードは暫定で1,500円）
- Stripeのテストモードで先に確認するか、本番モードへ進めるか
- 本番ドメイン
- 販売者情報と返金条件
- 海外販売の対象地域
